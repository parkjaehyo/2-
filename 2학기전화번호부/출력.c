#include<stdio.h>
void Output()
{


}
/*
void load_from_txt() {
    FILE* file = fopen("c언어 전화번호부.txt", "r");

    if (file == NULL) {
        printf("파일을 열 수 없습니다.");
        return;
    }

    char line[100];


    while (fgets(line, sizeof(line), file)) {
        printf("%s", line);
    }

    fclose(file);
}
*/